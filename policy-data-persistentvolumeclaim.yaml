apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: policy-data
  name: policy-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
